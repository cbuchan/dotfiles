- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.aliases: .aliases
    ~/.tmux.conf: .tmux.conf
    ~/.tmux.conf.line: .tmux.conf.line
    ~/.vim: .vim
    ~/.vimrc: .vimrc
    ~/.zshrc: .zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
